"""generate_future.py

Stub: generate future frames conditioned on current frames, masks and policy maps.
"""

def generate_future_frames(input_frame_dir: str, policy_map_dir: str, out_dir: str) -> None:
    print(f"(stub) generating future frames from {input_frame_dir} using {policy_map_dir} -> {out_dir}")


if __name__ == '__main__':
    print('Run generate_future.generate_future_frames(...)')
