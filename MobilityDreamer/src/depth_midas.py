"""depth_midas.py

Stub: compute depth maps using MiDaS and write to `data/`.
"""

def compute_depth(image_path: str, out_depth_path: str) -> None:
    print(f"(stub) MiDaS would compute depth for {image_path} -> {out_depth_path}")


if __name__ == '__main__':
    print('Run depth_midas.compute_depth(...)')
